<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window-level themes only; all real theming happens in Compose. -->
    <style name="Theme.DesignerBuddy" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- SplashScreen API theme, hands off to Theme.DesignerBuddy after launch. -->
    <style name="Theme.DesignerBuddy.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/ic_launcher_background</item>
        <item name="postSplashScreenTheme">@style/Theme.DesignerBuddy</item>
    </style>
</resources>
